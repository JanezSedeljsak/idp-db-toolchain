# Secrets and overrides — copy to .env (optional if using backupper.toml defaults)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Optional overrides (normally set in backupper.toml)
# BACKUPPER_CONFIG=backupper.toml
# APP_ENV=prod
# S3_BUCKET=my-prod-bucket

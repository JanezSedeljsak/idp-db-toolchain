# Secrets and overrides — copy to .env (optional if using db-toolchain.toml defaults)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Optional overrides (normally set in db-toolchain.toml)
# DB_TOOLCHAIN_CONFIG=db-toolchain.toml
# APP_ENV=prod
# S3_BUCKET=my-prod-bucket

apiVersion: v1
kind: ServiceAccount
metadata:
  name: backupper
  annotations:
    # Replace with your IAM role ARN (EKS IRSA). Terraform/AWS console creates this role.
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/idp-db-backupper

apiVersion: 1
providers:
  - name: db-toolchain
    orgId: 1
    folder: idp-db-toolchain
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards

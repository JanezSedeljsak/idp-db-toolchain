# Observability (prod EC2)

Docker Compose templates for a Prometheus + Grafana EC2 host alongside the platform EKS cluster. Local/k8s demo uses in-cluster `k8s/prometheus.yaml` and `k8s/grafana.yaml` instead.
